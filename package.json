{
  "name": "observable",
  "version": "0.0.0",
  "description": "Observable mixin using jQuery.Callbacks. CommonJS / browserify.",
  "main": "observable.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "browserify index.js -o bundle.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^4.2.0"
  }
}
